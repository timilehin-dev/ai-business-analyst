# Core dependencies
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.8.2
pydantic-settings==2.4.0

# Agent & LLM
langgraph==0.2.24
litellm>=1.44.1

# Database & Analytics
sqlalchemy==2.0.32
psycopg[binary]>=3.1

# Web Search (Newsroom)
duckduckgo-search==6.2.0
trafilatura==1.10.0
beautifulsoup4>=4.12.0

# NOTE: duckduckgo-search requires pyreqwest-impersonate>=0.5.0.
# Pin to 0.5.3 (last release with prebuilt wheels). Newer versions
# (0.5.5+) are source-only and require a Rust toolchain to build,
# which breaks Docker builds and adds ~20 minutes of compile time.
pyreqwest-impersonate==0.5.3

# Code Execution
docker>=7.0.0

# Security & Validation
sqlvalidator==0.0.2
cryptography>=42.0.0

# Utilities
httpx==0.27.2

# Development
pytest==8.3.2
pytest-asyncio==0.23.8
black==24.8.0
ruff==0.6.2