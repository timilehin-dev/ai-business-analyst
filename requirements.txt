# Core dependencies
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.8.2
pydantic-settings==2.4.0

# Agent & LLM
langgraph==0.2.24
litellm>=1.44.1

# Database & Analytics
sqlalchemy==2.0.32
psycopg[binary]>=3.1

# Web Search (Newsroom)
# NOTE: queries DuckDuckGo's HTML endpoint directly via httpx + BeautifulSoup.
# This avoids `duckduckgo-search`, which requires `pyreqwest-impersonate`
# (Rust-backed; source-only releases break Docker builds).
beautifulsoup4>=4.12.0

# Code Execution
docker>=7.0.0

# Security & Validation
sqlvalidator==0.0.2
cryptography>=42.0.0

# Utilities
httpx==0.27.2

# Development
pytest==8.3.2
pytest-asyncio==0.23.8
black==24.8.0
ruff==0.6.2